const create = ({distribution}) => {};

const update = (id, distribution) => {};

const del = (id) => {};

const read = (id) => {};

const readAll = () => {};

const execute = (id) => {};

module.exports = {
  create,
  update,
  del,
  read,
  readAll,
  execute,
};
