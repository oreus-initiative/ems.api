const add = ({distribution}) => {};

const update = (id, distribution) => {};

const del = (id) => {};

const get = (id) => {};

const getAll = () => {};

const execute = (id) => {};

module.exports = {
  add,
  update,
  del,
  get,
  getAll,
  execute,
};
