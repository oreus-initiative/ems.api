const add = ({cgRetributions}) => {};

const update = (id, cgRetributions) => {};

const del = (id) => {};

const get = (id) => {};

const getAll = () => {};

module.exports = {
  add,
  update,
  del,
  get,
  getAll,
};
