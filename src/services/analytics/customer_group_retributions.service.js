const create = ({cgRetributions}) => {};

const update = (id, cgRetributions) => {};

const del = (id) => {};

const read = (id) => {};

const readAll = () => {};

module.exports = {
  create,
  update,
  del,
  read,
  readAll,
};
