const create = ({cRetributions}) => {};

const update = (id, cRetributions) => {};

const del = (id) => {};

const read = (id) => {};

const readAll = () => {};

module.exports = {
  create,
  update,
  del,
  read,
  readAll,
};
