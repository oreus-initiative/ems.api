const add = ({cRetributions}) => {};

const update = (id, cRetributions) => {};

const del = (id) => {};

const get = (id) => {};

const getAll = () => {};

module.exports = {
  add,
  update,
  del,
  get,
  getAll,
};
